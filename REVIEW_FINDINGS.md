# Adversarial Review — material-flow change (feat/warehouse-autonomous-sim)

6 findings raised, 6 confirmed, 0 refuted. All verdicts high-confidence.

## [MAJOR] Cross-dock completes the inbound putaway Job with zero progress, wedging replenishment for that SKU
**Where:** lib/application/brains/putaway_robot_brain.dart:250  ·  **Dimension:** crossdock

**Defect:** On the cross-dock drop branch, ctx.board.completeJob(_jobId!) is called with the default progressUnits:0, but _jobId is the putaway Job that carries the INBOUND replenish Order's id (inbound_robot_brain.dart mints it with orderId:_orderId). The normal putaway branch credits that Order (line 263-264); cross-dock never does. Because the diverted pallet also never reaches the rack, the inbound Order can be closed by neither satisfaction (progress stays 0 < ordered) nor stock_monitor's !_lowAnywhere test (the rack is still low). It therefore lingers 'open' until the kStaleOrderTicks=600 stale timeout, and StockMonitorBrain.inFlight(sku) suppresses every further replenishment truck for that SKU during that whole window.

**Failure scenario:** Rack face for SKU 'A' is at quantity 0 (below reorder). StockMonitor mints inbound ORD-in (48 loose) and a manifest-1 truck. Outbound generator has an open order with an unclaimed pallet pick Job P for 'A'. Truck docks, inbound robot unloads and mints putaway Job J(orderId=ORD-in). The cart cross-docks: claims J and P, drives the pallet to outbound staging, and at drop completeJob(J) credits ORD-in nothing. ORD-in stays open with rack 'A' still 0, so inFlight('A') stays true and no replenishment truck is sent for 600 ticks. Rack 'A' stays empty; any subsequent outbound pallet demand for 'A' finds no stock, so those pick Jobs releaseOrFail 8x and their orders abort.

**Verifier (high conf):** Confirmed. In the real material flow the putaway Job always carries the inbound replenish Order's id: stock_monitor mints ORD-in + InboundTruckBrain(orderId=ORD-in); the truck mints unloadTruck(orderId=ORD-in) (inbound_truck_brain.dart:97); the inbound robot mints the putaway Job with orderId=_orderId (inbound_robot_brain.dart:120). On the cross-dock drop branch (putaway_robot_brain.dart:250) completeJob(_jobId!) is called with the default progressUnits:0. In completeJob (job_board.dart:496-514) the `if (order != null && progressUnits > 0)` guard is false, so ORD-in is neither advanced nor closed â€” it stays status=open with progress 0. The normal putaway branch (line 263-264) would have credited it. Because the pallet was diverted to outbound staging instead of the rack, the rack stays low, so stock_monitor's close paths both miss it: satisfaction fails (progress 0 < ordered) and !_lowAnywhere is false (rack still low) (stock_monitor_brain.dart:97). Only the kStaleOrderTicks=600 stale timeout (line 99) closes it; until then inFlight(sku) (line 51-54) stays true and every further replenishment truck for that SKU is suppressed. The truck brain only frees its bay on depart, never closing ORD-in. Reachable and in fact systemic: in the 5-robot bootstrap cast there is no dedicated pallet picker (sim_bootstrap.dart:42,77-84), so pallet picks are served exclusively by cross-dock, and every cross-docked pallet was staged from an inbound replenish truck, so its putaway Job always carries a live ORD-in.

---

## [MAJOR] Cross-dock mints packAndLoad into an Order aborted mid-flight, orphaning the Job and permanently stranding the pallet on a pack station
**Where:** lib/application/brains/putaway_robot_brain.dart:240  ·  **Dimension:** crossdock

**Defect:** The cross-dock drop branch calls stageOutbound (places a physical pallet on a pack-station cell), releases the stage reservation, and mints a packAndLoad Job with orderId:_xdockOrderId â€” without re-checking that the held pick Job's Order is still live. The held pick Job is only 'claimed' (never markActive), so nothing settles/sweeps it while the cart drives, but its Order can be aborted+swept meanwhile (OutboundTruckBrain kMaxSeekTicks abort, or a sibling line's pick Job hitting kMaxJobAttempts). The minted packAndLoad then references a dead Order; OutboundRobotBrain.perceiveAndDecide does `order == null => shipBay == null => continue`, so it never claims the Job. The Job is never claimed, never fails, never swept, and the pallet placed by stageOutbound is never taken from outboundStageProvider â€” that pack-station cell is occupied forever, so _freeStage/_findStage skip it permanently.

**Failure scenario:** Outbound order ORD has line L0 pallet(48) with unclaimed pick P_pallet and line L1 case(4) with pick P_case; the SKU has no case stock. A putaway cart cross-docks an inbound pallet of the SKU: claims P_pallet (holds it) and reserves a stage cell, then drives ~30 ticks. Concurrently a case picker claims P_case, finds no case stock, and releaseOrFail's it; after 8 attempts P_case fails and ORD is set aborted, then Phase-3 sweeps ORD. The cross-dock cart reaches drop, stageOutbound places the pallet on the pack station, mints packAndLoad Q(orderId=ORD, now gone). No outbound robot ever claims Q (order null -> shipBay null), and the pallet sits on that pack-station cell for the rest of the run, removing it from the staging pool; repeated occurrences exhaust all pack stations and wedge outbound staging.

**Verifier (high conf):** The cross-dock drop branch (putaway_robot_brain.dart:229-254) unconditionally stageOutbound + mints packAndLoad Q with orderId=_xdockOrderId, with NO re-check that the Order is still live â€” unlike _setupCrossDock (line 161-163) which does check. The held pallet pick Job survives sweep (sweepTerminal only removes done/failed, job_board.dart:544-547), but the Order can be aborted mid-drive by a reachable path: a sibling case line's pick failing after 8 attempts (pick_robot_brain.dart:94 -> releaseOrFail -> job_board.dart:465-473 sets ORD aborted), which fast-churns (~1 attempt/tick) to abort by ~tick 8-10, well inside the cart's ~30-tick drive; sweepTerminal then removes ORD that same tick (scheduler Phase 3). At drop the cart mints Q into the now-deleted Order. Q is orphaned on every axis: never claimed (OutboundRobotBrain order==null => shipBay==null => continue, outbound_robot_brain.dart:43-45, and it does NOT fail the job), never failed (nothing owns it to call releaseOrFail), never swept (unclaimed, not done/failed). stageOutbound placed the pallet in outboundStageProvider (action_applier.dart:136-139); the only removal is takeFromStage after a claim (line 144), so the pack-station cell stays occupied forever and _freeStage/_findStage (gated on held.isFree) skip it permanently. No global cleanup exists (grep: no order-gone job reclaim; stock_monitor is inbound-only). The happy-path p1_crossdock_test.dart keeps the Order open and never exercises abort mid-flight. Concrete input reaches wrong result with no guard.

---

## [BLOCKER] Aborted outbound Order strands its already-staged pallets, permanently leaking pack-station cells until the outbound loop wedges
**Where:** lib/application/brains/outbound_truck_brain.dart:72  ·  **Dimension:** wedge

**Defect:** When an outbound Order is aborted (truck seek-timeout at outbound_truck_brain.dart:72, or a sibling Job failing 8x at job_board.dart:461-473 which aborts the whole Order), any pallets already staged for that Order and their pack-station cells are never reclaimed: the packAndLoad Jobs become permanently unclaimable and outboundStageProvider keeps the cells held forever.

**Failure scenario:** wipCap=3, 2 outbound bays. Order O is minted; its pickers start immediately (picking does NOT wait for the truck to dock), stage 2 case pallets onto pack cells C1/C2, and mint 2 packAndLoad Jobs (pick_robot_brain.dart:179-199). O's OutboundTruckBrain cannot claim a bay within kMaxSeekTicks=120 (both bays occupied by other orders' trucks), so it runs closeOrder(O, aborted:true) at outbound_truck_brain.dart:72 and removes itself. O is now aborted and gets swept. The outbound robot only claims a packAndLoad while order!=null && order.shipBay!=null (outbound_robot_brain.dart:45); O's shipBay was never set (truck never docked) and O is now gone, so both packAndLoad Jobs are permanently unclaimable. takeFromStage is the ONLY caller that frees a staged cell (action_applier.dart:144) and it runs only on the packAndLoad success path, so C1/C2 stay held in outboundStageProvider forever (no recovery JobKind, no sweep-time reclaim; sweepTerminal only drops done/failed Jobs). The same strand happens via job_board.dart:461-473: a multi-line Order where one line's pick fails 8x flips the whole Order to aborted while the other lines' pallets are already staged. Each such abort -- exactly the DL-3 truck-timeout / stock-depletion events the design expects -- leaks 1+ pack cells. Once every packStation cell is held by stranded goods, _findStage (pick_robot_brain.dart:314-325) and _freeStage (putaway_robot_brain.dart:171-182) return null for every picker and every cross-dock, so every subsequent pick aborts at the picking->toStage transition, aborting still more Orders and staging still more strays: the outbound pick->stage->load pipeline is permanently wedged and the WIP cap fills with orders that can never stage.

**Verifier (high conf):** Confirmed reachable strand with no guard. Pickers stage pallets as soon as an outbound Order's pickToStage jobs are minted (outbound_order_generator_brain.dart:124-139), independent of the truck. Staging places a permanent hold on a pack cell via outboundStageProvider.place (action_applier.dart:137, from pick_robot_brain.dart:179) and mints a packAndLoad job (pick_robot_brain.dart:186-198). If the Order is then aborted, nothing releases either. In the truck seek-timeout case (outbound_truck_brain.dart:69-73), shipBay is set ONLY at driving->docked (outbound_truck_brain.dart:81) and the truck never docked, so O.shipBay stays null. OutboundRobotBrain claims a packAndLoad only while shipBay != null (outbound_robot_brain.dart:44-45), so the job is permanently unclaimable. sweepTerminal removes the aborted Order but KEEPS the unclaimed packAndLoad (it only drops done/failed jobs, job_board.dart:544-548), orphaning it. outboundStageProvider.take â€” the sole cell-free path (action_applier.dart:145) â€” runs only on the outbound robot success path (outbound_robot_brain.dart:102), which is never reached, so the cell is held forever. No reclaim exists: closeOrder/sweepTerminal never fail an order's outstanding jobs or free stage cells; JobKind.recovery is never minted or claimed and no recovery brain is registered in sim_bootstrap.dart. The DL-3 sibling-line 8x-fail abort (job_board.dart:461-473) reaches the same strand, and because pick claiming never checks order status, a dead order's surviving pickToStage jobs keep getting claimed and staged as new strays. This violates the invariant that rack/stage/cell reservations and held Jobs must be released on every exit path including abort â€” the abort path releases neither the stage cell nor the packAndLoad job.

---

## [MAJOR] Pick robot re-releases its rack reservation on offline, freeing another unit's reservation
**Where:** lib/application/brains/pick_robot_brain.dart:162  ·  **Dimension:** leaks-races

**Defect:** After the pick commits in _PK.picking the rack-source reservation is released (line 162) but `_rack` is left non-null; it stays set across the whole toStage+staging walk. onReset (goOffline) at lines 263-266 releases `_rack` a SECOND time, and BayOccupancyNotifier.release (bay_resource.dart:34) is ownership-blind, so the second release removes whatever unit now holds that rack cell.

**Failure scenario:** Pallet PickRobot A claims a pickToStage Job for SKU X, reserves rackPallet cell R (qty 2), and picks one unit: at pick_robot_brain.dart:160-162 it releases R's reservation but keeps `_rack = R`, then walks toward the stage over several ticks. While A walks, PutawayRobot C decides a putaway for SKU X; `_decideDestination` -> `_availablePalletRack` returns R (still SKU X, not full), and C reserves R via rackReservationProvider.claimFirstFree([R], C). A's battery hits 0 mid-walk -> scheduler calls goOffline -> onReset (line 262), which sees `_rack != null` and calls release(R.row, R.col), deleting C's reservation on R. A second putaway cart D can now reserve R too, and both C and D drop a pallet into R: the SBI-2/F2-fsm 'two carts cannot fill the same cell' invariant is broken (one drop is silently clamped to 0 absorbed, under-crediting the inbound Order). Fix: set `_rack = null` right after the release at line 162 so onReset never re-releases a cell the picker no longer owns.

**Verifier (high conf):** Confirmed in full. pick_robot_brain.dart:160-162 releases R's rack reservation but leaves `_rack = R` (only nulled in `_reset()` at 286); `_rack` persists through the toStage/staging walk. onReset (263-266), reached via goOffline (unit_brain.dart:212-219) on battery-0, releases `_rack` a SECOND time. BayOccupancyNotifier.release (bay_resource.dart:34-38) is ownership-blind â€” it deletes the cell key regardless of owner â€” and the ownership-aware releaseAllBy is never called on rackReservationProvider (grep-confirmed). rackReservationProvider persists across ticks (scheduler clears only cellReservationProvider). The window is real: after line 162 R is free in the provider, and a putaway cart's _availablePalletRack (putaway_robot_brain.dart:398-412) returns R because after the single-unit pick R keeps skuId X (pickFromRack/copyWith preserves skuId) and !isFull; the cart reserves R at line 79. When the picker drains to 0 mid-walk (the existing offline-recovery regression shows mid-haul drain is realistic), its stale onReset deletes the cart's reservation. With a second rack-filling unit (bootstrap round-robins _Slot.putaway and supports arbitrary robot counts; the SBI-2 reservation exists precisely for scaling), a second cart then reserves the freed R and both drop into it; dropToRack (action_applier.dart:83-97) clamps to maxQuantity so the second drop absorbs 0, clears cargo, and completeJob(progressUnits: 0) â€” lost inventory + under-credited order, breaking the F2-fsm/LCC-2 'two carts cannot fill one cell' invariant. Fix (null _rack at line 162) is correct; _rack has no legitimate use after 162 (only _abort, unreachable post-picking, and onReset reference it).

---

## [MAJOR] packAndLoad Job leaks and its staged pallet is stranded when the outbound Order aborts before load
**Where:** lib/application/job_board.dart:546  ·  **Dimension:** leaks-races

**Defect:** A packAndLoad Job is only claimable while its Order's shipBay is set (outbound_robot_brain.dart:45). If the Order aborts before that Job is claimed, nothing fails/sweeps the packAndLoad: sweepTerminal keeps every non-done/non-failed Job (lines 544-547), so the orphaned Job is scanned every tick forever and the pallet staged for it stays on the outbound stage cell permanently.

**Failure scenario:** OutboundTruckBrain for Order O cannot secure a bay and hits kMaxSeekTicks (outbound_truck_brain.dart:69-74), calling closeOrder(O, aborted: true) and removing itself â€” WITHOUT ever setting shipBay. Meanwhile a picker already picked one trip and, at pick_robot_brain.dart:186-199, staged its pallet on stage cell S and minted packAndLoad PL0 (orderId=O) which is still unclaimed because shipBay was never published. Phase-3 sweepTerminal removes O (aborted) but keeps PL0 (status unclaimed, not done/failed). From then on every OutboundRobot.perceiveAndDecide reads orders[O] == null -> shipBay null -> `continue`, so PL0 is never claimed, never fails, never swept; it accumulates in state.jobs and is re-scanned each tick, and the pallet at S sits in outboundStageProvider forever, permanently reducing stage capacity. The same orphan arises via the cross-dock mint at putaway_robot_brain.dart:240 or when a sibling pick Job fails 8x (releaseOrFail) and aborts O after a packAndLoad already exists.

**Verifier (high conf):** Confirmed unguarded leak. A packAndLoad Job is minted with orderId=O and its pallet physically placed in outboundStageProvider (pick_robot_brain.dart:177-199) with no check of O's status. If O's OutboundTruckBrain never secures a bay, it aborts via closeOrder(O, aborted:true) at outbound_truck_brain.dart:69-74 WITHOUT ever calling setShipBay, so shipBay stays null. OutboundRobotBrain.perceiveAndDecide (outbound_robot_brain.dart:43-46) reads order?.shipBay and `continue`s BEFORE board.claim when it is null (or when the swept order is gone), so the Job is never claimed, attempts never increments, and the kMaxJobAttempts=8 releaseOrFail watchdog never fires. sweepTerminal (job_board.dart:544-548) keeps every non-done/non-failed Job, so the unclaimed orphan persists and is re-scanned by claimableFor every tick forever, while the aborted Order O is pruned. No code reacts to an aborted order to fail child Jobs or clear staged pallets: stock_monitor only handles inboundReplenish (stock_monitor_brain.dart:94), the docked-truck abort branch (outbound_truck_brain.dart:90) is unreachable here since the truck never docked, and goOffline only releases a robot's own claimed currentJobId. outbound_stage.dart removes a pallet only via take (loading) or clear (bootstrap), neither of which fires. Result: leaked packAndLoad Job plus a permanently stranded pallet reducing outbound-stage capacity â€” violating the 'held Jobs/reservations released on every exit path' invariant on a path the code explicitly anticipates (the kMaxSeekTicks bay-starvation abort). No test covers abort-before-dock with a staged pallet.

---

## [MINOR] Line builder never coin-flips the first UOM: lowest-index servable UOM is in EVERY order and the isEmpty fallback is dead code
**Where:** lib/application/brains/outbound_order_generator_brain.dart:96  ·  **Dimension:** capacity-generator

**Defect:** The guard `if (lines.isNotEmpty && !rng.chance(0.5)) continue;` short-circuits on the first iteration (lines is empty), so the first servable UOM is always added without a coin flip. `available` is sorted by enum index (pallet=0, caseUom=1, loose=2), so the lowest-index servable UOM is force-included in every order rather than varied. Because `available` is guaranteed non-empty (the earlier `if (available.isEmpty) return;`), `lines` always has >=1 element after the loop, making the `if (lines.isEmpty)` fallback at line 105 unreachable dead code. The behavior contradicts the comment 'a coin-flip keeps order shapes varied, and the fallback below guarantees at least one line' â€” the author evidently intended every UOM (including the first) to be coin-flipped with the fallback catching the all-tails case; instead the first UOM is deterministic and the fallback can never run.

**Failure scenario:** 6-robot floor so servableUoms = {pallet, caseUom, loose}; SKU 'A' is stocked in rackPallet, rackCase and rackLoose. On every emit tick that selects A, available = [pallet, caseUom, loose]. First loop iteration: lines.isEmpty, so the guard short-circuits and a pallet line is appended with NO rng.chance draw. Result: 100% of A's outbound orders contain a pallet line (pallet demand never varied out), while case/loose appear ~50% each â€” not the intended even variety. The `if (lines.isEmpty)` fallback at line 105 is provably never executed. No invariant is violated (no wedge, no double-credit, determinism intact), but demand shaping and the fallback both deviate from the coded intent.

**Verifier (high conf):** Both factual assertions of the claim are provably true and unrefuted. (1) At line 96, `if (lines.isNotEmpty && !rng.chance(0.5)) continue;` â€” on the first loop iteration `lines` is empty, so `lines.isNotEmpty` is false and Dart's `&&` short-circuits, meaning `rng.chance(0.5)` is never even evaluated. The first UOM is therefore appended unconditionally with no coin-flip draw. Since `available` is sorted ascending by enum index (lines 85-86) and `enum UomKind { pallet, caseUom, loose }` (job_board.dart:22), the lowest-index servable UOM (pallet when servable+stocked) is force-included in EVERY order; case/loose are the only ones coin-flipped ~50%. (2) The `if (lines.isEmpty)` fallback at line 105 is genuinely unreachable dead code: line 87 (`if (available.isEmpty) return;`) guarantees the loop body runs at least once, and the first iteration always appends, so `lines` is always non-empty afterward. No guard, test, or downstream logic prevents either outcome â€” the two single-UOM tests (p4_outbound_loop, p4_outbound_loose) actually depend on the unconditional first-add and never exercise the fallback or assert distribution. The claim correctly scopes this as a minor demand-shaping/dead-code deviation from the stated comment intent, with no invariant (wedge/double-credit/determinism) violated. The concrete failure scenario reaches exactly the described behavior.

---
